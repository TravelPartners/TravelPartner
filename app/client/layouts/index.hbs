<!DOCTYPE html>

<html>
    <head>
        <meta charset="utf-8">
        <title> TravelPartner </title>

	      <link rel="stylesheet" href="//fonts.googleapis.com/css?family=Roboto:400,500,700,400italic|Material+Icons">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.10/css/all.css" integrity="sha384-+d0P83n9kaQMCwj8F4RJB66tzIwOKmrdb46+porD/OvrJ+37WqIM7UoBtwHO6Nlg" crossorigin="anonymous">

        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css" integrity="sha384-rHyoN1iRsVXV4nD0JutlnGaslCJuC7uwjduW9SVrLvRYooPp2bWYgmgJQIXwl/Sp" crossorigin="anonymous">

        <style type="text/css">
         .for-sign-up {
             display: none;
         }

         #sign-in-modal {
             top: 10%;

         }

         #sign-in-modal .row {
             background: white;
         }
        </style>

	      <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
        <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    </head>

    <body>
        <div id="nav-container">
            <nav class="navbar navbar-inverse navbar-fixed-top"  >
                <div class="container" >
                    <div class="navbar-header" >
                        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-nav-demo" aria-expanded="false">
                            <span class="sr-only">Toggle navigation</span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                        <a href="/" class="navbar-brand">TravelPartner</a>
                    </div>

                    <div class="collapse navbar-collapse" id="bs-nav-demo">
                        <ul class="nav navbar-nav">
                            <li><a href="############">Guide</a></li>
                            <li><a href="############">Food</a></li>
                            <li><a href="############">Accomodation</a></li>
                        </ul>
                        <ul class="nav navbar-nav navbar-right" id="beforeLogin">
                            <li><a href="#sign-in-modal" data-toggle="modal" data-target="#sign-in-modal"><i class="fas fa-user-circle"></i> &nbsp;Sign In/Register</a></li>
                        </ul>
                        <ul class="nav navbar-nav navbar-right" id="afterLogin" style="display:none;">
                            <li><a href="#" id="nbBtnMessageBox"><i class="fas fa-envelope"></i></a></li>
                            <li class="dropdown" id="userInfoArea">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                    <i class="fas fa-user-circle"></i> &nbsp;
                                    <span id="userName"></span>
                                </a>
                                <ul class="dropdown-menu">
                                    <li><a href="#"><i class="fas fa-book"></i> &nbsp;User profile</a></li>
                                    <li role="separator" class="divider"></li>
                                    <li><a href="#" id="btn-signout"><i class="fas fa-sign-out-alt"></i> &nbsp;Sign Out </a></li>
                                </ul>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
            <!-- Modal -->
            <div class="modal fade" id="sign-in-modal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                <div class="modal-dialog" role="document">
                    <div class="modal-content">
                        <div class="modal-header" style="background-color:#8585ad">
                          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
					                       &times;
				                  </button>
                            <h5 class="modal-title" id="myModalLabel" style="text-align:center;color: white; margin: 0.5em 0"> Login With </h5>
                        </div>
                        <div class="modal-body">
                            <form class="container-fluid">
                                <div class="row alert alert-danger" style="display:none;margin: 1em 0" id="signInError">
                                </div>
                                <div class="row" style="margin: 1em 0">
                                    <div class="col-md-8 col-md-offset-2">
                                        <input type="text" class="form-control" id="inputName" placeholder="Username/Email" >
                                    </div>
                                </div>
                                <div class="row for-sign-up" style="margin:1em 0">
                                    <div class="col-md-8 col-md-offset-2">
                                        <input type="text" class="form-control" id="inputEmail" placeholder="Email" >
                                    </div>
                                </div>
                                <div class="row" style="margin:1em 0">
                                    <div class="col-md-8 col-md-offset-2">
                                        <input type="password" class="form-control" id="inputPassword" placeholder="Password" >
                                    </div>
                                </div>
                                <div class="row for-sign-up" style="margin:1em 0">
                                    <div class="col-md-8 col-md-offset-2">
                                        <input type="password" class="form-control" id="inputRetypedPassword" placeholder="Retype your password" >
                                    </div>
                                </div>
                                <div class="row" style="margin:1em 0">
                                    <div class="col-md-8 col-md-offset-2" style="padding:0 2em;font-size:0.6em;">
                                    <a href="" id="to-register" class="under-form" style="float:left"> No account? Register </a>
                                    <a href="" id="to-forget-password" class="under-form" style="float:right"> Forgot password</a>
                                    </div>
                                </div>
                                <div class="row" style="text-algin:center;margin:1em 0;clear:both;">
                                    <div class="col-md-4 col-md-offset-4" style="text-align:center;">
                                        <button type="button" class="btn btn-primary" id="btn-signin" style="width:80%">Sign In</button>
                                    </div>
                                </div>
                            </form>
                        </div>
                        <div class="modal-footer">
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div id="body-container">
            {{{ body }}}
        </div>
    </body>
    <script type="text/javascript">
     $(document).ready(function() {
         // TODO: Change it. Check status each time listener is triggered.
         // var lastScroll = 0;
         // $(window).scroll(function() {
         //    var thisScroll = $(this).scrollTop();
         //    if (thisScroll > lastScroll) {
         //        $('#nav-container').hide(200);
         //    } else {
         //        $('#nav-container').show(200);
         //    }
         //    lastScroll = thisScroll;
         // });

         var islogin = true;

         let nameInStorage = localStorage.getItem('username');
         if (nameInStorage != undefined) {
             $('#userInfoArea #userName').text(nameInStorage);
             $('#beforeLogin').hide();
             $('#afterLogin').show();
         }


         $('#btn-signin').click(function(event){
             event.preventDefault();

             var name = $('#inputName').val();
             var pwd = $('#inputPassword').val();

             var signInErrorText = '';
             $('#signInError').text('');

             if (name == undefined || name == '')
                 signInErrorText += 'Name cannot be blank. <br/>';

             if (pwd == undefined || pwd == '')
                 signInErrorText += 'Password cannot be blank. <br>';

             if (islogin) {
                 if (signInErrorText != '') {
                     $('#signInError').html(signInErrorText);
                     $('#signInError').show();
                     return false;
                 }
                 axios.post('/s/signin', {
                     username: name,
                     pwd: pwd
                 }).then(function (resp) {
                     console.log(resp);
                     if (resp.data.status == 'success') {
                         console.log(resp);
                         localStorage.setItem('token', resp.data.token);
                         localStorage.setItem('avatar', resp.data.avatar);
                         localStorage.setItem('username', name);

                         $('#userInfoArea #userName').text(name);
                         $('#beforeLogin').hide();
                         $('#signInError').hide();
                         $('#afterLogin').show();
                         $('#sign-in-modal').modal('hide');

                     } else if (resp.data.status == 'err') {
                         //console.log(err);
                         $('#signInError').text(resp.data.msg);
                         $('#signInError').show();
                     }
                 }).catch(function(err) {
                     console.log(err);
                 });
             } else {
                 var email = $('#inputEmail').val();
                 var retypedPwd = $('#inputRetypedPassword').val();

                 if (email == undefined || email == '')
                     signInErrorText += 'Email cannot be blank. <br>';
                 if (retypedPwd == undefined || retypedPwd == '')
                     signInErrorText += 'Retyped password cannot be blank. <br>';

                 if (pwd != undefined && retypedPwd != undefined && pwd != '' && retypedPwd != '' && pwd != retypedPwd)
                     signInErrorText += 'Retyped password did not match the password.';

                 if (signInErrorText != '') {
                     $('#signInError').html(signInErrorText);
                     $('#signInError').show();
                     return false;
                 }

                 axios.post('/s/signup', {
                     username: name,
                     pwd: pwd,
                     email: email
                 }).then(function (resp) {
                     console.log(resp);
                     if (resp.data.status == 'success') {
                         console.log(resp);
                         localStorage.setItem('token', resp.data.token);
                         localStorage.setItem('avatar', resp.data.avatar);
                         localStorage.setItem('username', resp.data.username);

                         $('#userInfoArea #userName').text(name);
                         $('#beforeLogin').hide();
                         $('#signInError').hide();
                         $('#afterLogin').show();
                         $('#sign-in-modal').modal('hide');
                     } else if (resp.data.status == 'err') {
                         //console.log(err);
                         $('#signInError').text(resp.data.error.msg);
                         $('#signInError').show();
                     }
                 }).catch(function(err) {
                     console.log(err);
                 });
             }
         });

         $('#btn-signout').click(function(event){
             event.preventDefault();
             console.log('123');

             $('#userInfoArea #userName').text('');
             $('#beforeLogin').show();
             $('#signInError').hide();
             $('#afterLogin').hide();

             localStorage.removeItem('username');
             localStorage.removeItem('token');
             localStorage.removeItem('avatar');
         });

         $('#to-register').click(function(event) {
             event.preventDefault();

             islogin = (islogin == true ? false : true);
             if (islogin) {
                 $('#sign-in-modal #myModalLabel').html('Log with');
                 $('#inputName').attr('placeholder', 'Username/email');
                 $('.for-sign-up').hide();
                 $('#btn-signin').html('Sign in');
             } else {
                 $('#sign-in-modal #myModalLabel').html('Register');
                 $('#inputName').attr('placeholder', 'Username');
                 $('.for-sign-up').show();
                 $('#btn-signin').html('Register');
             }
        });

     });
    </script>
</html>
